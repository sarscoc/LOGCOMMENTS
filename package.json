{
  "name": "trpg-log-marker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler pages dev public --d1 DB=trpg-log-marker-db",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
